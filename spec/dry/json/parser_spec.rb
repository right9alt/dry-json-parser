# frozen_string_literal: true

RSpec.describe Dry::Json::Parser do
  it "has a version number" do
    expect(Dry::Json::Parser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
